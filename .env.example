# 首次部署必填：初始管理员密码（至少 12 位，仅首次启动无用户时生效）
INITIAL_ADMIN_PASSWORD=

# 消息推送凭据主密钥（可选，node scripts/generate-push-key.js 生成；配置推送供应商前必须设置）
PUSH_CONFIG_MASTER_KEY=

# 过机设备对接密钥（可选，配置后 /api/machine/weigh 才可用）
MACHINE_API_KEY=
